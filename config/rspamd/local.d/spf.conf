# Swaks-injected lab traffic arrives from loopback/compose-net IPs, which
# rspamd classifies as "local" and skips by default; check anyway so
# Authentication-Results carries real verdicts.
check_local = true;
