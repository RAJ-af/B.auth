use = ["authentication-results", "x-spam-status"];
# The authentication-results routine is withheld for local-network sources by
# default; our lab injects everything over loopback/compose-net, so disable
# the skips to get real verdicts stamped on every message.
skip_local = false;
skip_authenticated = false;
